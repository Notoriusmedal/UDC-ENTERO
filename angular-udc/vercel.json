{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci",
  "buildCommand": "npm run build && rm -rf dist-vercel && cp -R dist/angular-udc/browser dist-vercel",
  "outputDirectory": "dist-vercel",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
